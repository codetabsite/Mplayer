<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- TV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />

    <application
        android:name=".MPlayrApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:banner="@mipmap/ic_launcher">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- TV launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/*" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.NowPlayingActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.StatsActivity"
            android:exported="false" />

        <!-- [18] Çöp Kutusu -->
        <activity android:name=".ui.TrashActivity" android:exported="false" />

        <!-- [27] Klasör Kara Listesi -->
        <activity android:name=".ui.BlacklistActivity" android:exported="false" />

        <!-- [19] Kopya Şarkı Temizleyici -->
        <activity android:name=".ui.DuplicateCleanerActivity" android:exported="false" />

        <!-- Lyrics Editor 2.0 -->
        <activity android:name=".ui.LyricsEditorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- [30] Ringtone Maker -->
        <activity android:name=".ui.RingtoneMakerActivity" android:exported="false" />

        <!-- Tag Editor -->
        <activity android:name=".ui.TagEditorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Folder Explorer -->
        <activity android:name=".ui.FolderExplorerActivity" android:exported="false" />

        <!-- Playlist Manager -->
        <activity android:name=".ui.PlaylistActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Community Playlists -->
        <activity android:name=".ui.CommunityPlaylistActivity" android:exported="false" />

        <!-- Power User Mode -->
        <activity android:name=".ui.PowerUserActivity" android:exported="false" />

        <!-- Android TV Mode -->
        <activity
            android:name=".ui.TvActivity"
            android:exported="false"
            android:theme="@style/AppTheme"
            android:launchMode="singleTop" />

        <service
            android:name=".service.PlayerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <receiver
            android:name=".service.SleepTimerReceiver"
            android:exported="false" />

    </application>
</manifest>
