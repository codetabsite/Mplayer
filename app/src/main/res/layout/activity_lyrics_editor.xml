<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/lyricsRootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg">

    <!-- Toolbar -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="56dp"
        android:orientation="horizontal" android:gravity="center_vertical">
        <ImageButton android:id="@+id/btnBack"
            android:layout_width="48dp" android:layout_height="48dp"
            android:src="@drawable/ic_back"
            android:background="?attr/selectableItemBackgroundBorderless"/>
        <TextView android:id="@+id/tvLyricsSongTitle"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/text_primary"
            android:textSize="15sp"
            android:fontFamily="sans-serif-medium"
            android:ellipsize="end"
            android:singleLine="true"/>
    </LinearLayout>

    <!-- Current playback position -->
    <TextView android:id="@+id/tvCurrentPos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Playback position: 00:00.00"
        android:textColor="@color/accent"
        android:textSize="13sp"
        android:padding="8dp"
        android:gravity="center"/>

    <!-- Lines container — RecyclerView injected here programmatically -->
    <LinearLayout
        android:id="@+id/lyricsLinesContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"/>

    <!-- Input row -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp">
        <EditText android:id="@+id/etNewLine"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Type lyric line…"
            android:textColor="@color/text_primary"
            android:imeOptions="actionDone"
            android:maxLines="2"/>
        <Button android:id="@+id/btnAddLineNow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Add Now"
            android:layout_marginStart="6dp"/>
    </LinearLayout>

    <!-- Action buttons -->
    <Button android:id="@+id/btnClearLyrics"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:text="Clear All"
        android:textSize="13sp"
        android:backgroundTint="#44FF5252"/>
</LinearLayout>
